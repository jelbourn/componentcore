{
  "name": "componentcore",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ./dist && tsc -p tsconfig.json && cp package.json ./dist"
  },
  "devDependencies": {},
  "dependencies": {
    "typescript": "^3.9.6"
  }
}
