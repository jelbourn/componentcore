/**
 * Whether the library/aplication is running in development mode.
 * Use this to gate anything that should be stripped away for production
 * deployments. This is the equivalent to Closure's `DEBUG` or Angular's
 * `ngDevMode`.
 */
export const devMode = true;
